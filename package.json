{
  "private": true,
  "name": "tab-organizer",
  "version": "5.0.0-beta",
  "devDependencies": {
    "babel-core": "^5.8.21",
    "babel-loader": "^5.3.2",
    "webpack": "^1.11.0"
  },
  "dependencies": {
    "gsap": "^1.18.0"
  },
  "scripts": {
    "build": "npm install && webpack --bail -p",
    "watch": "npm install && webpack --watch"
  }
}
