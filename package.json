{
  "private": true,
  "name": "tab-organizer",
  "version": "5.0.0-beta",
  "devDependencies": {
    "browserify": "^10.2.4",
    "babelify": "^6.1.2",
    "babel-core": "^5.6.4",
    "watchify": "^3.2.2"
  },
  "scripts": {
    "build": "npm install && ./bin/build.sh",
    "watch": "npm install && ./bin/watch.sh"
  }
}
