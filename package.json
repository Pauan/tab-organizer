{
  "private": true,
  "name": "tab-organizer",
  "version": "5.0.0-beta",
  "scripts": {
    "build": "rimraf static/js tab-organizer.zip && rollup --config && node zip.js"
  },
  "devDependencies": {
    "@wasm-tool/rollup-plugin-rust": "^1.0.0",
    "archiver": "^4.0.2",
    "rimraf": "^2.6.3",
    "rollup": "^1.27.14",
    "rollup-plugin-terser": "^5.1.3"
  }
}
