{
  "private": true,
  "name": "tab-organizer",
  "version": "5.0.0-beta",
  "scripts": {
    "build": "cargo web build --target wasm32-unknown-unknown --release && node build.js release",
    "test": "cargo web test --target wasm32-unknown-unknown"
  }
}
