{
  "private": true,
  "name": "tab-organizer",
  "version": "5.0.0-beta",
  "devDependencies": {
    "babel-core": "^5.8.25",
    "babel-loader": "^5.3.2",
    "babel-runtime": "^5.8.25",
    "webpack": "^1.12.2"
  },
  "scripts": {
    "build": "npm install && webpack --bail -p",
    "watch": "npm install && webpack --watch"
  }
}
